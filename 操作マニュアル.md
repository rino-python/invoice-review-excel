# 操作マニュアル（請求書の転記チェック）

対象：Excel は使えるが、プログラムは使わない方。所要時間は約3分です。

## 準備

- 請求書が `.txt`、`.md`、または文字を選択できる `.pdf` であること
- スキャンした画像だけの PDF は、このデモでは読めません

手元にファイルが無いときは、左の「サンプル一式」を使ってください。サンプルには、正しい請求書・税計算のずれ・期限の前後・番号の重複・項目欠けが混ざっています。

## 操作

1. アプリを開く
2. （任意）左の「サンプル一式」を保存して中身を見る
3. 請求書を左にドロップする。または「同梱サンプルで試す」にチェックする（一度に20件まで）
4. 「読み取って検算する」を押し、件数と生成AIの呼び出し回数を確認して「実行する」
5. 「要確認」「転記一覧」タブで中身を確認する
6. 「結果Excelを保存」を押す

生成AIは必須ではありません。空欄のまま実行すると、規則と検算だけで動きます。

## 結果の見方

判定の基準は、結果 Excel の「判定基準」シートにも書いてあります。

| 判定 | 基準 | 画面・Excelでの色 |
|---|---|---|
| 転記可 | 必須項目があり、税計算・日付・番号重複に問題がなく、生成AIの補完もない | 緑 |
| 要確認 | 欠け・税のずれ・期限の前後・番号重複・生成AI補完のいずれか | 赤 |

- **要確認タブ**：先に見る場所。理由が行末に付きます
- **転記可の税込**：要確認を除いた合計。支払いの下書きには、要確認を直してから使ってください
- **項目の取得方法**：`プログラムだけ` は規則で取った行。`プログラム＋生成AIで補完` は、抜けていた項目を生成AIが埋めた行です。補完した行は要確認のままです

税抜と消費税が両方あるときは、その合計と税込を比べます。1円以上ずれると要確認です。消費税が空で税抜と税込があるときは、税抜の10%込みで検算します。

## うまくいかないとき

| 表示 | 確認すること |
|---|---|
| 請求書を上げてください | ファイルを選ぶか、同梱サンプルにチェックする |
| 生成AIの認証に失敗しました | APIキーを確認する。ChatGPT Plus の契約だけでは使えません |
| 生成AIに接続できませんでした | ネット接続と OpenAI の障害情報を確認する |
| 文字を取れませんでした | スキャン画像ではなく、文字をコピーできる PDF / テキストにする |
| 対応形式は .txt / .md / .pdf | Excel の請求書一覧はこのツールの入力ではありません |
| 一度に処理できるのは 20 件までです | ファイルを分けて上げる |

## 対象外

スキャン画像の OCR、会計ソフトへの自動登録、振込の実行は範囲外です。必要なら別見積もりになります。
